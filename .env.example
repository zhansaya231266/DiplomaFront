VITE_API_URL=http://localhost:8080/v1
# Optional: preferred endpoints are tried first, then built-in fallbacks.
# VITE_DEPARTMENTS_ENDPOINTS=/organizations/departments,/departments
# VITE_POSITIONS_ENDPOINTS=/organizations/positions,/positions
